package cs3500.reversi.view;

/**
 * An interface to represent the view of a game of Reversi. An interface is necessary because
 * it can be implemented in different classes for different types of views. We currently have
 * a textual view, and we can implement other classes that represent different, more complex
 * views.
 */
public interface ReversiView {
  // Our view currently just has a toString, so we did not need additional methods, but more
  // will be added.
}
